@model IEnumerable<AgendCont.Models.Tipo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripition)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripition)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoId }, new { @class = "btn btn btn-warning" }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoId }, new { @class = "btn btn btn-info" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoId }, new { @class = "btn btn btn-danger" })
        </td>
    </tr>
}

</table>
